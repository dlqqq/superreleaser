# pixi-managed environments (pixi.lock IS committed for reproducibility)
.pixi/

# Airflow local home: SQLite db, logs, generated admin password
.airflow/

# Locally cloned conda-forge feedstocks (cloned on demand by the prepare phase)
feedstocks/

# Locally cloned source repos (cloned on demand to edit jupyter-ai's pyproject)
sources/

# Python
__pycache__/
*.pyc
.pytest_cache/
